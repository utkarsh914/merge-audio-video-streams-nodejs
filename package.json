{
  "name": "merge-audio-video-streams",
  "version": "1.0.0",
  "description": "Merge video only and audio only streams during delivery to save storage for videos having audios in multiple languages",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Utkarsh Tiwari",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "ejs": "^3.1.3",
    "express": "^4.17.3",
    "fluent-ffmpeg": "^2.1.2"
  }
}
